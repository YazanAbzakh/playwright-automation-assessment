{
  "name": "playwright-automation",
  "version": "1.0.0",
  "description": "Playwright TypeScript framework for SauceDemo UI and Simple Books API automation.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/ui --project=\"Google Chrome - UI\" --project=\"Mozilla Firefox - UI\"",
    "test:api": "playwright test tests/api --project=\"Simple Books API\"",
    "test:headed": "playwright test tests/ui --project=\"Google Chrome - UI\" --project=\"Mozilla Firefox - UI\" --headed",
    "typecheck": "tsc --noEmit",
    "report": "playwright show-report reports/html"
  },
  "keywords": [
    "playwright",
    "typescript",
    "automation",
    "testing",
    "qa"
  ],
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
